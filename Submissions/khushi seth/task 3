#html

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ZeroTheory</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <button class="theme-toggle" onclick="toggleTheme()">Toggle Theme</button>

  <header>
    <h1>ZeroTheory</h1>
    <nav>
      <a href="#features">Features</a>
      <a href="#community">Community</a>
      <a href="#contact">Contact</a>
    </nav>
  </header>
  <section class="hero">
    <canvas id="heroCanvas"></canvas>
    <div class="hero-content">
      <h2>Explore the Future</h2>
      <p>ZeroTheory brings you futuristic designs, interactive dashboards, and a community-driven experience.</p>
      <button onclick="alert('Get Started!')">Get Started</button>
    </div>
  </section>

  <section class="features" id="features">
    <div class="feature">
      <h3>Futuristic UI</h3>
      <p>Experience a sleek and modern interface designed for intuitive navigation and interactive engagement.</p>
    </div>
    <div class="feature">
      <h3>Community Driven</h3>
      <p>Join a thriving community where ideas come to life, and collaboration is at the heart of everything.</p>
    </div>
    <div class="feature">
      <h3>Interactive Dashboard</h3>
      <p>Monitor your projects, track progress, and get insights through an interactive dashboard built for productivity.</p>
    </div>
  </section>

  <footer>
    &copy; 2026 ZeroTheory. All rights reserved.
  </footer>

  <script src="script.js"></script>
</body>
</html>

#CSS

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: 'Poppins', sans-serif;
}

body {
  background-color: #0d0d0d;
  color: #fff;
  overflow-x: hidden;
  transition: background-color 0.5s, color 0.5s;
}


.theme-toggle {
  position: fixed;
  top: 20px;
  right: 20px;
  background: #fff;
  color: #0d0d0d;
  border: none;
  padding: 10px 15px;
  border-radius: 8px;
  cursor: pointer;
  font-weight: bold;
  transition: all 0.3s;
  z-index: 10;
}

.theme-toggle:hover {
  transform: scale(1.1);
}


header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px 50px;
  border-bottom: 1px solid rgba(255,255,255,0.1);
  position: relative;
  z-index: 5;
}

header h1 {
  font-size: 2rem;
  letter-spacing: 2px;
  color: #00ffea;
}

nav a {
  color: #fff;
  text-decoration: none;
  margin-left: 20px;
  font-weight: 500;
  transition: color 0.3s;
}

nav a:hover {
  color: #00ffea;
}


.hero {
  position: relative;
  height: 90vh;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

#heroCanvas {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
}

.hero-content {
  position: relative;
  text-align: center;
  z-index: 5;
}

.hero h2 {
  font-size: 3rem;
  color: #00ffea;
  text-shadow: 0 0 10px #00ffea, 0 0 20px #00ffea;
  animation: fadeInMove 2s ease-out forwards;
}

.hero p {
  margin: 20px 0 40px;
  font-size: 1.2rem;
  max-width: 600px;
  opacity: 0;
  animation: fadeInMove 2s ease-out 0.5s forwards;
}

.hero button {
  padding: 15px 30px;
  font-size: 1rem;
  background: #00ffea;
  border: none;
  border-radius: 50px;
  cursor: pointer;
  transition: all 0.3s;
}

.hero button:hover {
  background: #00c8b0;
  transform: scale(1.05);
}

@keyframes fadeInMove {
  0% {
    opacity: 0;
    transform: translateY(30px);
  }
  100% {
    opacity: 1;
    transform: translateY(0);
  }
}


.features {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 40px;
  padding: 80px 50px;
  text-align: center;
}

.feature {
  background: rgba(0,255,234,0.1);
  padding: 30px;
  border-radius: 15px;
  transition: transform 0.3s, box-shadow 0.3s;
}

.feature:hover {
  transform: translateY(-10px);
  box-shadow: 0 10px 30px rgba(0,255,234,0.3);
}

.feature h3 {
  margin-bottom: 15px;
  color: #00ffea;
}


footer {
  background: #001f2d;
  text-align: center;
  padding: 30px;
  font-size: 0.9rem;
  color: #aaa;
}

body.light {
  background-color: #f5f5f5;
  color: #0d0d0d;
}

body.light header h1,
body.light .hero h2,
body.light .feature h3 {
  color: #0d0d0d;
}

body.light .theme-toggle {
  background: #0d0d0d;
  color: #fff;
}

body.light .hero button {
  background: #0d0d0d;
  color: #fff;
}

body.light .hero button:hover {
  background: #333;
}

body.light .feature {
  background: rgba(0,0,0,0.05);
}
#JS


function toggleTheme() {
  document.body.classList.toggle('light');
}


const canvas = document.getElementById('heroCanvas');
const ctx = canvas.getContext('2d');
canvas.width = window.innerWidth;
canvas.height = window.innerHeight;
let stars = [];
const numStars = 150;

for(let i=0; i<numStars; i++){
  stars.push({
    x: Math.random()*canvas.width,
    y: Math.random()*canvas.height,
    radius: Math.random()*1.2,
    speed: Math.random()*0.5
  });
}

function animate() {
  ctx.clearRect(0,0,canvas.width,canvas.height);
  stars.forEach(star => {
    star.y -= star.speed;
    if(star.y < 0) star.y = canvas.height;

    ctx.beginPath();
    ctx.arc(star.x, star.y, star.radius, 0, Math.PI*2);
    ctx.fillStyle = "#00ffea";
    ctx.fill();
  });

  requestAnimationFrame(animate);
}

animate();
window.addEventListener('resize', () => {
  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;
});

